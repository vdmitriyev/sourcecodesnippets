services:

  go-cli-bundle:
    build:
      context: .
      dockerfile: Dockerfile
    tty: true
    stdin_open: true
    environment:
      - TERM=xterm
    volumes:
      - ./data:/app # Replace with the actual path on your host machine
    working_dir: /app
    command: /bin/bash
